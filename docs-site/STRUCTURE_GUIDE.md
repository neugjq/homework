# Docusaurus 网站结构说明

## 📁 文件结构对应关系

### 1. 首页（Landing Page）
- **文件**: `src/pages/index.js`
- **样式**: `src/pages/index.module.css`
- **访问路径**: `https://your-site.com/` (根路径)
- **说明**: 这是网站的首页，显示渐变背景和大标题

### 2. 文档页面（Docs）
- **文件夹**: `docs/`
- **配置**: `sidebars.js` (控制侧边栏结构)
- **访问路径**: `https://your-site.com/docs/...`

#### 文档文件与侧边栏的对应关系：

```
docs/
├── intro.md                          → 侧边栏: "基本信息" > "简介"
├── tutorial-basics/                  → 侧边栏: "从这里开始"
│   ├── congratulations.md           → "Congratulations!"
│   ├── create-a-blog-post.md        → "Create a Blog Post"
│   ├── create-a-document.md         → "Create a Document"
│   ├── create-a-page.md             → "Create a Page"
│   ├── deploy-your-site.md          → "Deploy your site"
│   └── markdown-features.mdx        → "Markdown Features"
└── tutorial-extras/                  → 侧边栏: "上手案例"
    ├── manage-docs-versions.md      → "Manage Docs Versions"
    └── translate-your-site.md       → "Translate your site"
```

### 3. 侧边栏配置（sidebars.js）

```javascript
const sidebars = {
  tutorialSidebar: [
    {
      type: 'category',           // 分类类型
      label: '基本信息',           // 侧边栏显示的名称
      collapsed: false,           // 是否默认折叠
      items: [
        'intro',                  // 对应 docs/intro.md
      ],
    },
    {
      type: 'category',
      label: '从这里开始',
      collapsed: false,
      items: [
        {
          type: 'autogenerated',  // 自动生成
          dirName: 'tutorial-basics',  // 对应 docs/tutorial-basics/ 文件夹
        },
      ],
    },
    // ... 更多分类
  ],
};
```

### 4. 导航栏配置（docusaurus.config.js）

```javascript
navbar: {
  title: 'AAG中文文档',           // 左上角标题
  items: [
    { to: '/docs/intro', label: '指南', position: 'left' },      // 导航链接
    { to: '/docs/intro', label: 'API文档', position: 'left' },
    { href: 'https://github.com/...', label: 'GitHub', position: 'right' },
  ],
}
```

---

## 🔧 如何修改内容

### 1. 修改首页
编辑 `src/pages/index.js` 和 `src/pages/index.module.css`

### 2. 添加新文档页面
1. 在 `docs/` 文件夹中创建新的 `.md` 文件
2. 在文件顶部添加 frontmatter：
```markdown
---
sidebar_position: 1
title: 页面标题
---

# 页面内容
```

### 3. 修改侧边栏结构

#### 方法 A：手动添加单个文档
```javascript
// sidebars.js
items: [
  'intro',           // docs/intro.md
  'getting-started', // docs/getting-started.md
  'api-reference',   // docs/api-reference.md
]
```

#### 方法 B：自动生成整个文件夹
```javascript
items: [
  {
    type: 'autogenerated',
    dirName: 'tutorial-basics',  // 自动包含该文件夹下所有文件
  },
]
```

#### 方法 C：创建嵌套分类
```javascript
{
  type: 'category',
  label: '高级教程',
  items: [
    'advanced/topic1',
    'advanced/topic2',
    {
      type: 'category',
      label: '子分类',
      items: ['advanced/sub/item1', 'advanced/sub/item2'],
    },
  ],
}
```

### 4. 删除侧边栏项目
- 从 `sidebars.js` 中删除对应的条目
- 或者删除 `docs/` 文件夹中的对应文件

### 5. 修改导航栏
编辑 `docusaurus.config.js` 中的 `navbar.items`：
```javascript
items: [
  { to: '/docs/intro', label: '文档', position: 'left' },
  { to: '/blog', label: '博客', position: 'left' },
  { href: 'https://github.com/...', label: 'GitHub', position: 'right' },
]
```

---

## 📝 实际示例

### 示例 1：添加新的 API 文档页面

1. 创建文件 `docs/api-reference.md`：
```markdown
---
sidebar_position: 2
title: API 参考
---

# API 参考

这里是 API 文档内容...
```

2. 更新 `sidebars.js`：
```javascript
{
  type: 'category',
  label: '基本信息',
  items: [
    'intro',
    'api-reference',  // 新增这一行
  ],
}
```

### 示例 2：创建新的文档分类

1. 创建文件夹 `docs/advanced/`
2. 在其中添加文件：
   - `docs/advanced/configuration.md`
   - `docs/advanced/deployment.md`

3. 更新 `sidebars.js`：
```javascript
{
  type: 'category',
  label: '高级指南',
  collapsed: false,
  items: [
    {
      type: 'autogenerated',
      dirName: 'advanced',
    },
  ],
}
```

### 示例 3：删除不需要的教程

1. 删除文件夹 `docs/tutorial-extras/`
2. 从 `sidebars.js` 中删除对应部分：
```javascript
// 删除这个部分
{
  type: 'category',
  label: '上手案例',
  items: [...]
}
```

---

## 🎨 文件类型说明

- `.md` - Markdown 文档
- `.mdx` - Markdown + React 组件
- `.js` - React 组件页面
- `.module.css` - CSS 模块（局部作用域）
- `docusaurus.config.js` - 全局配置
- `sidebars.js` - 侧边栏配置

---

## 🔗 URL 路径规则

- `docs/intro.md` → `/docs/intro`
- `docs/tutorial-basics/create-a-page.md` → `/docs/tutorial-basics/create-a-page`
- `src/pages/about.md` → `/about`
- `src/pages/index.js` → `/` (首页)
